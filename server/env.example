PORT=5000                  # (optional) local server port
NODE_ENV=development        # development | production

# Database
MONGODB_URI=your-mongodb-connection-string

# Auth
JWT_SECRET=your-jwt-secret
JWT_EXPIRE=7d

# Cloudinary (required only if you use image uploads)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Optional runtime/public URLs (used by client/server deployments)
# CLIENT_URL_PROD=https://your-client.example.com
# PUBLIC_API_URL=https://your-server.example.com/api

